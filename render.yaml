services:
  - type: web
    name: mothra
    runtime: python
    rootDir: landing-page
    buildCommand: > 
      npm install && npm run build && 
      pip install -r scripts/requirements.txt
    startCommand: cd scripts && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MOTHRA_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: mothra-db
          property: connectionString

databases:
  - name: mothra-db
    databaseName: mothra
    plan: free