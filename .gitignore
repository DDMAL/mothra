# Adjust this as necessary while project evolves
staff-finding/config.yaml
# macOS metadata — never track these
.DS_Store
**/.DS_Store

runs/
outputs/
predictions*/
staff-finding/page_tests
staff-finding/grouping_tests
staff-finding/scripts/__pycache__/
staff-finding/models/
.claude/
zzzz/
staff-finding/addtl-gt/

# Personal per-user config for the DDMAL HuggingFace sync tooling — hardcodes
# absolute paths specific to the machine it was generated on (see the file's
# own header comment).
staff-finding/config.yaml
# Editable-install build artifacts (any package, anywhere in the repo)
*.egg-info/

# Downloaded pretrained weights
yolov8*.pt
!models/**/*.pt

# Training logs
training_output.log
*.log

# Symlinks
data/labels

#.venv
landing-page/scripts/.venv


#any kind of config file
.hfconfig

# local environment variables (contain secrets — never commit)
.env
landing-page/scripts/.env
text-service/.venv

# artifacts from component filtering
*.pyc

#dependencies
landing-page/node_modules
landing-page/scripts/__pycache__
landing-page/scripts/mock_data
landing-page/scripts/.venv
landing-page/scripts/stored_models
text-service/.python-version
paco-classifier-service/.venv
paco-classifier-service/__pycache__

# Neon build output and runtime session manifests (generated by npm run build:neon)
landing-page/public/neon/

# Vite build output
landing-page/dist/
landing-page/.vite/

# Claude Code local config
.claude/

.idea

# Kubernetes secrets — real values, never commit. Use the templates instead:
# cp k8s/secret.yaml.example         k8s/secret.yaml           (production)
# cp k8s/staging/secret.yaml.example k8s/staging/secret.yaml   (staging)
k8s/secret.yaml
k8s/staging/secret.yaml