fastapi>=0.110.0
uvicorn>=0.29.0
numpy<2
Pillow>=10.0.0
python-jose[cryptography]>=3.3.0
pydantic>=2.0.0
bcrypt>=4.0.0
python-multipart>=0.0.9
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
ultralytics
scipy
scikit-image
# Already arrives transitively via ultralytics, and declared again by
# staff-finding's pyproject -- listed explicitly because pitch_stage.py's
# algorithm now imports cv2 directly (via the submodule's glyph_pixels), so
# dropping ultralytics would otherwise silently disable pitch finding.
# opencv-python, NOT -headless: both provide `cv2` and installing both in one
# environment is a known conflict (see staff-finding/pyproject.toml).
opencv-python
slowapi
huggingface_hub>=0.20.0
celery[redis]>=5.3.0
redis>=5.0.0
PyYAML>=6.0
